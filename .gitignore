# macOS
.DS_Store
.AppleDouble
.LSOverride

# Nix
result
result-*
.direnv/
.envrc

# Home Manager
home-manager-generation

# Logs
*.log

# Secrets (just to be safe)
*.key
*.pem
*.crt
secrets/
.env
.env.*

# Note: config/fonts is a git submodule pointing to private fonts repo
# The submodule reference must be tracked, but the actual fonts are restricted

# Shell history files (should never be committed - may contain secrets)
*_history

# Temporary files (unlikely, but just in case)
*.tmp
*.bak
*.backup
*.old
tmp/
temp/
